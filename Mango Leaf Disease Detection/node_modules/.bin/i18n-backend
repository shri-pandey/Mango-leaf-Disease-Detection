#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../i18n-backend/lib/src/i18n-cli.js" "$@"
else 
  exec node  "$basedir/../i18n-backend/lib/src/i18n-cli.js" "$@"
fi
